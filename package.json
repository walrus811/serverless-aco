{
  "name": "serverless-aco",
  "description": "serverless-aco",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
    "aws-sdk": "^2.799.0"
  },
  "devDependencies": {
    "cpy-cli": "^4.1.0",
    "jest": "^26.6.3"
  },
  "scripts": {
    "i:env": "npx cpy env_default.json . --rename=env.json",
    "test": "jest"
  }
}
